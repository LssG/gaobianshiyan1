实验三
连接流程：
	服务器开启，等待客户端连接。
	客户端开启，向客户端发起连接。
	客户端向服务器发送房间号。*1
	服务器接收房间号。
	服务器存储客户端的fd，ip，房间号。
聊天流程：
	客户端从控制台获取消息。
	客户端在消息之前添加姓名。
	客户端向服务器发送调制过的消息。
	服务器接收到客户端的消息。
	服务器查询客户端所在的房间。
	服务器向房间中的所有客户端转发消息。
	客户端接收到消息后显示消息
服务器流程：
	服务器开启，等待客户端连接。
	当有一个新客户端连接时服务器创建一个接收消息的线程。